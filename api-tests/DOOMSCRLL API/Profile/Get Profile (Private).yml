info:
  name: Get Profile (Private)
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/profile/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
