info:
  name: Delete Profile (Private)
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/profile/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
