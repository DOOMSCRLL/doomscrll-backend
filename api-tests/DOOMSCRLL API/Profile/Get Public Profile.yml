info:
  name: Get Public Profile
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/profile/:username"
  params:
    - name: username
      value: notymc
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
